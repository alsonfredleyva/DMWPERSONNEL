# Use a MongoDB Atlas connection string for production or Atlas testing.
# Example:
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.u4ijswb.mongodb.net/dmwrox?retryWrites=true&w=majority
# If Atlas connection fails, the server falls back to local MongoDB at mongodb://127.0.0.1:27017/dmwrox.
MONGODB_URI=mongodb://127.0.0.1:27017/dmwrox
PORT=5000
